<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Mail.View.EditListView"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding ListName}" FontSize="Medium" TextColor="Black" HorizontalOptions="Center"></Label>
            <StackLayout Orientation="Horizontal">
                <Entry HorizontalOptions="Start" WidthRequest="250"></Entry>
                <Button HorizontalOptions="End" WidthRequest="70" HeightRequest="30" Text="Add"></Button>
            </StackLayout>
            <ListView ItemsSource="{Binding RecipiantsList}"></ListView>
            <Button HorizontalOptions="Center" Text="Ok"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>